<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.se.dao.UserDao">
    <select id = "findByUsernameAndPassword" resultMap="UserPO">
        select * from user where `name` = #{username} and password = #{password}
    </select>

    <select id = "findByUsername" resultMap="UserPO">
        select * from user where `name` = #{username}
    </select>

    <select id="findAllUser" resultType="com.se.model.po.UserPO">
        select * from user
    </select>

    <insert id = "createUser" parameterType="UserPO" useGeneratedKeys="true" keyProperty="id">
        insert into user (password, `name`, `role`)
        values (#{password}, #{name}, #{role})
    </insert>


    <resultMap id="UserPO" type="com.se.model.po.UserPO">
        <id column="id" property="id"></id>
        <result column="name" property="name"></result>
        <result column="password" property="password"></result>
        <result column="role" property="role"></result>
    </resultMap>
</mapper>